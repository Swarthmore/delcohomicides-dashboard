import makeStyles from '@mui/styles/makeStyles';

export const useStyles = makeStyles(() => ({
  root: {
    minWidth: 500,
    minHeight: 500,
    height: "100%",
    width: "100%",
    flexGrow: 1,
  },
  content: {
    width: "100%",
    height: "100%",
    flexGrow: 1,
  },
}));
